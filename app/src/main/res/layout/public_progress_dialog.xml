<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/dialog_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/transparent_black"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="300dp"
            android:layout_height="200dp"
            android:layout_centerInParent="true"
            android:background="@drawable/public_dialog_shape"
            android:gravity="center"
            android:minWidth="100dp"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/progress_dialog_img"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="28dip"
                android:src="@mipmap/loading" />

            <TextView
                android:id="@+id/progress_dialog_txt"
                android:layout_width="wrap_content"
                android:layout_height="@dimen/login_editText_hight"
                android:layout_marginTop="28dip"
                android:text="@string/plealse_wait"
                android:textColor="#292415"
                android:textSize="@dimen/textSize28px" />
        </LinearLayout>
    </RelativeLayout>
</RelativeLayout>
